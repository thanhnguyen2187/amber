import { CommonInputNumberDirective } from './common-input-number.directive';

describe('CommonInputNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonInputNumberDirective();
    expect(directive).toBeTruthy();
  });
});
