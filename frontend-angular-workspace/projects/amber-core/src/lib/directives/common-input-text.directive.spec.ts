import { CommonInputTextDirective } from './common-input-text.directive';

describe('CommonInputTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonInputTextDirective();
    expect(directive).toBeTruthy();
  });
});
