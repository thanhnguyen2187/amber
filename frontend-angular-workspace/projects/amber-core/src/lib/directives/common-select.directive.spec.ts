import { CommonSelectDirective } from './common-select.directive';

describe('CommonSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonSelectDirective();
    expect(directive).toBeTruthy();
  });
});
