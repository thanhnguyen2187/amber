import { CommonInputCheckboxDirective } from './common-input-checkbox.directive';

describe('CommonInputCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonInputCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
